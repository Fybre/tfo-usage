# TFO Usage Reports (internal)

Ingest periodic `TfoUsageReport-*.xlsx` files into SQLite and provide pages for upload/search/expiry/forecast.

## Docker

cd tfo-usage
docker compose up –build

Open http://localhost:8000
